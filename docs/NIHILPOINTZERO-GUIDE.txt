===============================================================================
           NIHILPOINTZERO STUDIO — THE COMPLETE PLAIN-ENGLISH GUIDE
     Everything you can do, how to do it, what's free, and what to expect.
                        Written to be easy to understand.
===============================================================================

CONTENTS
   1. What this is / first launch
   2. The one rule + moving it to another PC (what travels, what you redo)
   3. The tabs (what each is for) + Ctrl-K + autosave
   4. Make a video — step by step
   5. The build choices: Look, Format (shape), Resolution
   6. Voice — 4 ways (yours is best)
   7. Captions / subtitles
   8. Music & the DJ studio (make music, background, remove/replace, separate, waveform)
   9. Scene Studio (storyboard + put yourself in a scene + Beautify)
  9b. Storyboard Director (direct it shot by shot)
  9c. Timeline Editor (line clips up by hand)
  9d. Live PSX Data & NCCPL Analysis (real numbers → video)
  9e. Presenter Studio (put YOU in the video) + the ⏺ Recorder
  10. Thumbnails · Logo/brand · Publish to YouTube · Batch
  11. Free vs Internet vs one-time Setup
  12. FAQ (the dumb questions)
  13. Troubleshooting
  14. What's already set up for you
  15. Pro tips — get the best results
  16. Everything this studio can do (quick index)
  17. NEW: the AI outage, free music, the trim bar, and what this PC can't do
  18. NEW: REAL AI video — free cloud today, local GPU when the hardware exists

-------------------------------------------------------------------------------
1. WHAT THIS IS / FIRST LAUNCH
-------------------------------------------------------------------------------
You type (or speak) an idea; the app writes a script, makes the visuals, adds a
voice, music and captions, and gives you a finished video — or you record your own
voice over it. Most of it is FREE and offline. The smart-AI bits use the internet
(also free — keys are built in).

TWO WAYS TO RUN IT:
  PORTABLE (the classic way): double-click NIHILPOINTZERO-OS-portable.exe ONCE and
  wait ~40-60s (no window shows while it unpacks — normal). This is the copy that
  travels on a USB with your whole folder.
  INSTALLED (recommended for daily use on THIS PC): run NIHILPOINTZERO-OS-setup.exe
  once. From then on the app opens in ~2 seconds, taskbar pins are SAFE, and it uses
  the SAME nihilpointzero-data folder — your videos/settings appear in both versions.
If SmartScreen warns: More info -> Run anyway (it's safe, just unsigned). The
"NIHILPOINTZERO-OS" Desktop shortcut points at the portable exe.

WHICH BUILD AM I RUNNING? The sidebar shows a small gold badge under "OS", e.g.
"v0.1.1 · 2026-07-19 15:59 · 4e5e704" — that's version · the exact date+time the
build was made · a code id. It is stamped AUTOMATICALLY when the app is built, so it
can never be wrong or forgotten; it is proof of which code is running.
IMPORTANT (PORTABLE EXE ONLY): do NOT pin the portable app to the taskbar/Start
WHILE it's open — Windows freezes that pin to a temporary unpacked copy and it will
launch OLD code forever. Launch from the Desktop shortcut or the .exe in this folder.
The INSTALLED version has no such trap — pin it freely. (The pin trap once made hours
of updates look like "nothing changed".) See §13 if a build looks stale.

-------------------------------------------------------------------------------
2. THE ONE RULE + MOVING TO ANOTHER PC
-------------------------------------------------------------------------------
Your work lives inside the "nihilpointzero-data" folder next to the .exe. ALWAYS
copy the WHOLE "NihilPointZeroStudio" folder — never just the .exe — or your work
won't be found.

Copy the folder to another PC and:
  TRAVELS AUTOMATICALLY: all videos, scripts, Script Pad, library, settings, the
     built-in free keys, and the Natural voice (if downloaded).
  NEEDS INTERNET (no install): AI writing/images/visuals, put-me-in-a-scene, online
     music removal.
  YOU REDO ONCE (optional): offline Demucs music separation — it's a Python program
     tied to a machine. On the new PC: install Python (python.org), then in a command
     window run  python -m pip install demucs numpy , then Settings -> "Music
     separation (local)" -> type  python -m demucs  -> Save. You do NOT need this if
     you have internet (the Online button already removes music).

-------------------------------------------------------------------------------
3. THE TABS + CTRL-K + AUTOSAVE
-------------------------------------------------------------------------------
  Ideas & Trends  - brainstorm ideas with AI.
  ✦ AI Command    - tell the studio what to do in plain words; it plans and does it.
                    Also holds BATCH (many videos at once).
  🎬 Scene Studio  - build a video scene-by-scene; watch each, pause, reorder, and
                    put YOUR photo into a scene.
  Script Writer   - generate a full script.
  Script Pad      - a blank page to write/paste any script.
  Video Studio    - the main workshop: turn a script into a video and, per video:
                    Captions, Logo, 🎙 Voice studio, Trim, Music, Publish, Download.
                    Sub-tabs: Build & Videos, Sound Studio (DJ), AI Director.
  🎞 Storyboard Director - direct a video SHOT BY SHOT: each shot has its own length,
                    its own image prompt, optional narration/caption, camera motion,
                    a crossfade, and you can drop YOUR photo into a shot. (See §9b.)
  🎥 Presenter Studio - put YOU in the video, three ways: Real Video (upload a video of
                    yourself narrating), Photo (your photo presents), or ✨ Living
                    Picture (your video's motion grafted onto your best picture). (§9e.)
  ⏺ Recorder      - record your webcam OR your screen inside the app (up to 8K, noise
                    suppression, OBS virtual camera). Saves into Video Studio. (§9e.)
  ✂ Timeline Editor - a simple non-linear editor: line up video clips on a track, set
                    crossfades, add text overlays and a separate audio track. (See §9c.)
  Charts          - candlestick + SMA/RSI charts. Load LIVE PSX prices by symbol, or
                    import your own CSV/Excel. Can turn a chart into a narration script.
  📈 Live PSX Data - type a PSX symbol (LUCK, HUBC, ENGRO…) → real end-of-day data from
                    the PSX portal → in-app analysis → download Excel → reasoned script
                    → video. You choose language + what to focus on. (See §9d.)
  🏦 NCCPL Analysis - download a FIPI/LIPI (or any) file from NCCPL yourself and UPLOAD it
                    here → it's analysed → narration script in your language. (See §9d.)
  Advisor         - a chat that knows your work.
  Library         - everything you saved.
  Activity Log    - history of actions.
  Settings        - AI choice, keys, natural-voice download, music separation, AI Video
                    (the real-motion video engines, §18), your YouTube channel, phone
                    access, a "Setup health" panel (green dots = ready), and the
                    "Known Issues" log (AI failures AND tab crashes, provable).

  CTRL-K: press it anywhere, type a tab name, Enter — instant navigation.
  AUTOSAVE: your work saves itself and comes back after you close/reopen. A
     "Saving… / Saved ✓" note shows the status. Nothing is lost — the last few
     seconds of edits are flushed even if you switch tabs or close mid-type.
  ONLY YOU CAN DELETE: every delete (a video, a saved item, a shot, an advisor
     message, or "Clear Tab") pops a confirm box first. The AI can create and edit,
     but it can NEVER delete your work — that's yours alone.
  🎤 MIC: every text box has a mic button — click and talk instead of typing (offline).
  TOASTS: actions pop a little confirmation (or error) at the bottom so you always
     know what happened.
  🎬 PRODUCER: a floating AI panel on every screen. THREE modes — ADVISE (ask a
     YouTube-growth question about your current draft), EDIT (tell it "tighten the hook",
     "rewrite in Roman Urdu"; it proposes a rewrite you Apply), and DO IT (say what you
     want made — "write a 1-min script on gold and build it in 1080p" — and it turns that
     into a validated plan you approve with "Run it", then actually runs it: writing
     scripts, building videos, thumbnails, images, music, ideas, PSX analysis, scenes).
     It reads your current script/brief, and falls back to the free AI brain if your
     provider is down. It only performs that safe set of actions — it never deletes.
     ASK IT HOW: the Producer also knows this whole studio — every tab, button and
     workflow. On any screen ask "how do I put myself in a scene?", "where is the clip
     tool?", "why did my video not appear?" and it answers with the exact steps for THIS
     app (not generic advice). Two buttons at the top right of its panel set the answer
     length: 📖 Detailed (full step-by-step) or ⚡ Brief (quick bullets).
  🧭 EXPERT: a SECOND floating AI on every screen (the button at the BOTTOM-LEFT,
     next to the sidebar), SEPARATE from the Producer. It's the app's dedicated expert — it does
     nothing but know this software inside-out and help you use it:
     - ASK ANYTHING, ANY WAY: "detailed bullet points", "step wise", "precise steps
       naming every click", "explain fully", "keep it brief" — pick a format chip at
       the top or just say the format in your question; it obeys exactly.
     - ⚡ EXECUTE THESE STEPS: under each answer, one click turns the explanation into
       a real, validated action plan (write scripts, build videos, scenes, images,
       thumbnails, music, ideas, PSX analysis). NOTHING runs until you press "▶ Run it".
     - EXECUTE MODE: or write the orders yourself ("make a 30-second script about gold
       and build it vertical") and it plans + runs them the same way.
     Steps that are pure UI clicks (open a tab, toggle a setting) it explains precisely
     but cannot press for you — and like everything AI in this app, it can never delete.
     - → TAKE ME THERE: when an answer mentions a tab, chips appear under it ("→ Open
       Scene Studio") — one click jumps you straight to that tab.
  ⬆ UPDATE NOTICES: on startup the app quietly checks if a newer version was published.
     If yes, a small blue notice appears with a "Show me the file" button that opens the
     studio folder with NIHILPOINTZERO-OS-setup.exe selected — one double-click updates.
     Offline? The check silently skips; it never blocks or nags.
  💾 BACKUPS (Settings → Backups — and automatic): your work is copied to
     C:\Users\<you>\NihilPointZero-Backups (it moved there from Documents; the app moves
     the old folder for you automatically). The app backs up by itself at most once every
     7 days; "Back up now" in Settings does it on demand, and BACKUP-NOW.cmd in the
     Desktop studio folder still works too. It only adds/updates, never deletes.
     RESTORE (the part that matters in a disaster): Settings → Backups → "Restore missing
     files" copies back anything that is in the backup but MISSING from the app. It never
     overwrites anything you currently have — completely safe to press.
     SECOND HOME: pick a folder on a USB stick or second disk and every weekly backup is
     copied there too. A backup on the same disk cannot survive a disk failure — this one
     can. If the drive is unplugged that week, it's skipped and noted, never an error.
     DELETE-SYNC (on by default): when you permanently delete a video in the app, its
     backup copy is deleted too — deleted means GONE FOR GOOD, no ghost copies left
     behind. Turn it off in Settings → Backups if you'd rather backups keep everything.
     "Clean deleted-items ghosts" removes backup copies of things you deleted BEFORE
     delete-sync existed — it shows you the count and size first, and asks.
     WHAT IS AND ISN'T COPIED: videos, thumbnails, scripts, library, drafts and logs —
     with NO size limit, so your biggest finished videos are protected. Your API keys and
     the app's browser data are deliberately EXCLUDED (the backup folder may be cloud-
     synced, and in the portable copy saved keys can be read back — they must not travel).
     If any file can't be copied, the Activity Log says INCOMPLETE and it retries next
     time you open the app — it never claims success it didn't achieve.
  🩺 "RUN FULL CHECK" (Settings, top-right of Setup health): the most useful button in the
     app when something feels off. It really tests everything — internet, the free AI, the
     free image service, Ollama (and whether your chosen model is installed), and whether
     your saved Anthropic/OpenAI key is actually ACCEPTED. A wrong key turns RED with the
     reason. Green = truly working, not just "saved".
     AUTOMATIC TOO: the same check quietly runs by itself about once a week. If it finds
     a real problem, a small RED DOT appears on "Settings" in the sidebar and a plain-
     English line lands in the Activity Log — problems announce themselves instead of
     waiting to be discovered mid-project.
  🈳 DISK-SPACE GUARD: if your disk is almost full, video builds now say so up front
     ("This disk is almost full…") instead of failing halfway with a confusing error —
     and warn you early when space is merely getting low.
  🏠 TODAY (first sidebar item): the app opens here — latest videos, recent activity, and
     one-click cards to start the usual jobs. Ideas & Trends is now the item just below it.
  🏷 POSTING TEXT (Video Studio, under each video): one click writes a title, description
     and hashtags for that clip, each with its own Copy button — made for pasting straight
     into YouTube/TikTok after cutting shorts.

-------------------------------------------------------------------------------
4. MAKE A VIDEO — STEP BY STEP
-------------------------------------------------------------------------------
Easiest: ✦ AI Command -> type/🎤 "Write a 1-minute script about the rupee and build
it in 1080p with calm music" -> "Plan it" -> "▶ Run it". It appears in Video Studio.

Hands-on: Video Studio -> "Build & Videos" -> choose/paste a script -> set Look,
Format, Resolution, Voice, Music -> "🎬 Build Video". A live % and a preview image
show progress; the finished video lands on the right with all its tools.

-------------------------------------------------------------------------------
5. THE BUILD CHOICES
-------------------------------------------------------------------------------
VIDEO LOOK (engine) — what makes the visuals. Five tiers:
   Style presets              - free, offline. Styled text, backgrounds and your own
                                images. Always works, no key.
   Photo slideshow (AI images) - free, online, no key. A real AI image per scene,
                                panned and zoomed. A moving photo slideshow — not
                                filmed motion.
   REAL AI video — free cloud - free, online. REAL moving video per scene — not a
                                slideshow. Two free routes, chosen in Settings ->
                                AI Video: a free Pollinations key (Quest Pollen,
                                signup by GitHub/email — NO phone number) or a free
                                Puter account (Google Veo, sign-in window; their
                                phone verification rejects some countries' numbers,
                                Pakistan included). Up to 5 scenes per build get
                                real motion (adjustable) and the rest use AI stills;
                                each real-motion scene takes minutes. If it can't
                                run, scenes fall back to AI stills and the build
                                log says why. (See §18.)
   AI footage (cloud)         - paid; real AI footage from a provider you supply your
                                own key for.
   REAL AI video — local GPU (ComfyUI) - free per video and fully private, but needs a
                                dedicated NVIDIA card. On this PC it is visible but
                                greyed out: "Requires NVIDIA GPU — not detected on
                                this system". (See §18.)
LOOK (template):
   Clean     - plain and fast.
   News      - crisp colour + sliding "lower-third" bars showing the section label.
   Cinematic - colour-graded + vignette + film grain + black cinema bars (letterbox).
   Bold      - punchy colours.
   All add a smooth animated title, and section titles slide up as they appear.
FORMAT (shape): 16:9 = YouTube · 9:16 = Shorts/Reels/TikTok · 1:1 = square.
RESOLUTION: 1080p (fastest) · 1440p · 4K (sharp, slower) · 8K (very slow, huge).
   4K is the sweet spot. For Shorts pick the 9:16 shape.

-------------------------------------------------------------------------------
6. VOICE — 4 WAYS (yours is best; two are free natural Urdu options)
-------------------------------------------------------------------------------
  (A) YOUR OWN voice (recommended): build the video, then "🎙 Voice studio" under it:
        Record with ⏸ Pause / ▶ Resume; after Stop, drag the slider to review;
        "Redo from playhead" keeps the part before and re-records the rest;
        finish with "Replace the narration" OR "Keep existing audio + add my voice".
  (B) WINDOWS NATURAL voice (free, no download): pick "Narration voice -> Windows
        natural voice". Windows itself ships free voices per installed language —
        including real Urdu (Pakistan): Asad and Uzma. If you don't see them, click
        "+ Add Urdu / more voices" (in the picker, or Settings) to install the free
        Urdu language pack in Windows, then reopen this tab. "🔊 Hear this voice"
        lets you preview before committing.
  (C) NATURAL voice (Piper): a nice computer voice with NO Windows setup needed.
        Pick and download a voice in Settings — includes two real Urdu neural
        voices (male "Fasih", female "Aegis", ~60 MB each) alongside English ones.
        Then pick "Narration voice -> Natural voice (Piper)" when building.
  (D) WINDOWS voice: basic/robotic, always free, good for a quick draft.

-------------------------------------------------------------------------------
7. CAPTIONS / SUBTITLES
-------------------------------------------------------------------------------
On any built video, the "📝 Captions" row:
   "Get subtitles (.srt)" - a subtitle file; add it when uploading to YouTube.
   "Burn into video"      - bakes the words on-screen (great for muted Shorts).
It transcribes your narration automatically and offline.

-------------------------------------------------------------------------------
8. MUSIC & THE DJ STUDIO
-------------------------------------------------------------------------------
Video Studio -> "Sound Studio (DJ)":
   - Library: generated music moods, sound effects, a bundled pack, your own files,
     and "Find free music online" (legal, no-copyright search).
   - Timeline: place sounds (start time, volume, use only a slice).
   - "🎵 Create music only": render the timeline to an MP3 and SAVE it (no video).
   - "Apply mix -> new video": lay your sounds over a video's narration.
   - "📊 Show waveform": see the audio of the selected video (loud/quiet spots).

Remove or swap the background music:
   - Videos MADE IN THIS APP: the "🎵 Music" row -> Remove (keeps your voice) or
     Replace with a mood. Replace AUTO-DUCKS: the music dips whenever you speak.
     Exact and instant, no internet.
   - OUTSIDE videos (music already mixed in): "Remove music (AI separate)":
       Online = works out of the box (free, built in), needs internet.
       Local  = offline, best quality (Demucs, installed on this PC).
     Separation is an AI estimate — great for clear speech over music, not perfect.

-------------------------------------------------------------------------------
9. SCENE STUDIO (storyboard + put yourself in a scene)
-------------------------------------------------------------------------------
🎬 Scene Studio builds a video one scene at a time:
   - "Plan scenes" turns your script into scene cards (each with an editable prompt).
   - "Generate all" makes an image per scene — watch them appear. ⏸ Pause / ▶ Resume.
   - STORYBOARD: reorder scenes (↑ ↓), remove (✕), or "＋ Add a scene". The order you
     see is the order the video is built in.
   - Rewrite any scene's prompt and "↻ Regenerate this scene".
   - "📎 Put me in (photo)": attach your picture and describe the scene ("me as an
     astronaut, spacesuit, on the moon"). Slider "Photo transform" — lower keeps more
     of your real look. Free; uses internet; the queue can be slow (shows its place).
     Exact face likeness isn't guaranteed on free AI.
   - "🎬 Build video" stitches the approved scenes with narration, your Look, etc.
   - "VIDEO LOOK" selector: the final build can use your generated stills (classic,
     default) or REAL AI motion per scene — free cloud (Pollinations key or Puter sign-in) or local
     GPU (needs NVIDIA). Same rules and honest catches as Video Studio's engine
     list (§5, §18).
   - BEAUTIFY: a slider retouches your uploaded photo (skin-smooth + brighten + sharpen)
     on your REAL pixels — no invented face. This is a light retouch, not AI makeup.

-------------------------------------------------------------------------------
9b. STORYBOARD DIRECTOR (direct it shot by shot)
-------------------------------------------------------------------------------
🎞 Storyboard Director is for full creative control of the visuals, beat by beat.
   - AUTO mode: paste a whole script (and a title) and the AI DECIDES everything —
     topic, mood, genre, style, and the whole shot breakdown.
   - GUIDED mode: you write the shots yourself, e.g. "0-15s I arrive in a Ferrari and
     say '…'; then 20s a helicopter over the hills, slow fade; then 90s of me speaking".
   Each SHOT has: a length in seconds, a SCENE description (what to show), optional
   narration and an on-screen caption, a camera MOTION, a crossfade INTO it, and its own
   SOUNDS (add a music bed, a sound effect, or your own audio file — each with gain,
   fade-in/out and a start offset). You can reorder shots (↑↓) and delete them (with a
   confirm).
   SUBJECT per shot — who's on screen: (a) Scene only (AI/stock scene), (b) MY PHOTO —
   your real photo is beautified, background-removed and composited INTO the AI scene
   (your true face, not a fake), (c) MY CLIP — your own footage, (d) AI PERSON — a
   generated character (described by you; not your real face).
   BEAUTIFY: "Set my photo", then a slider — RIGHT smooths skin / brightens / evens tone,
   LEFT makes it grittier & sharper. It edits YOUR real pixels (a retouch, not AI makeup).
   Preview it before rendering.
   Build makes each shot, holds it for the right length (narration decides the length, so
   speech is never cut off or overlapped), crossfades between shots, lays narration +
   captions + your sounds, and opens the finished film in the ✂ Timeline Editor so you can
   fine-tune anything.
   SCENES SELECTOR: by default AI scenes are animated stills, but the "Scenes:"
   selector can switch them to REAL AI video — free cloud (Pollinations key or Puter sign-in, a few
   scenes per build, minutes per scene) or local GPU (needs NVIDIA). See §18.
   HONEST LIMITS (free): unless you pick a REAL-video engine above, AI scenes are
   STILLS with Ken-Burns motion, not real moving footage — and even with the free
   cloud engine, scenes past the per-build cap use AI stills.
   Your real face is kept by compositing your ACTUAL photo/clip — the app never
   fabricates a fake video of your face. The background cutout uses a small free model that
   downloads once on first use; if it can't (offline), it falls back to your photo framed
   in the scene, so a render never breaks.

-------------------------------------------------------------------------------
9c. TIMELINE EDITOR (line clips up by hand)
-------------------------------------------------------------------------------
✂ Timeline Editor is a real non-linear editor. Add video clips (or images) onto a
   track, trim them, reorder them, set a crossfade between clips, add timed text overlays
   (with fades), and place audio on a separate track with gain + fade-in/out — with a
   LIVE WAVEFORM for each audio clip. The mix has a peak limiter so it never clips. It
   renders to one MP4. A Storyboard build opens here automatically (all its shots, sounds
   and captions), so you can add / edit / remove ANY sound or clip after the AI first pass.

-------------------------------------------------------------------------------
9d. LIVE PSX DATA & NCCPL ANALYSIS (real numbers → video)
-------------------------------------------------------------------------------
📈 Live PSX Data: type a PSX symbol (LUCK, HUBC, ENGRO…). The app pulls the stock's
   REAL end-of-day history straight from the PSX data portal and computes — in-app,
   with standard formulas — the latest price + move, 52-week range, 1-year change,
   20/50/200-day moving averages, RSI(14), volume vs average, and a trend read.
   Then: "⬇ Excel" saves an auditable workbook; the "what & how + Language" box +
   "✍ Script" writes a reasoned narration (English / Roman Urdu / Urdu) using ONLY the
   computed numbers (it won't invent figures); "🎬 Build" turns it into a video.
   The maths is exact and tested; the fetch depends on the portal being reachable.

🏦 NCCPL Analysis: NCCPL's portal blocks bots, so this is UPLOAD-based — download the
   FIPI/LIPI (or any market) file from NCCPL yourself and upload it here. The app auto-
   detects and analyses it (foreign vs local net flow, or price/fundamentals), then —
   with the same what/how + Language box — writes a narration script and can build a
   video. All figures come from YOUR file.

Charts tab also does live PSX now: type a symbol → a close-price line + SMA/RSI, or
   import your own OHLC file for full candlesticks; an optional prompt turns it into a
   script too.

-------------------------------------------------------------------------------
9e. PRESENTER STUDIO (put YOU in the video) + THE ⏺ RECORDER
-------------------------------------------------------------------------------
🎥 Presenter Studio makes YOU the presenter. Pick a mode, add your video (or photo)
   and your script; the app intercuts you with theme b-roll and AI scenes, your voice
   throughout. Three modes:
   - REAL VIDEO (recommended): upload a video of yourself narrating. Your REAL voice
     becomes the master track and your real face appears on the on-camera moments —
     the rest cuts to theme b-roll + AI scenes timed to your voice. No fakery, real
     sync.
   - PHOTO: no video needed — your still photo appears (subtly moving, background-
     removed into the scene) on the presenter beats; the natural voice narrates. Good
     when you don't want to film yourself.
   - ✨ LIVING PICTURE: add your narration VIDEO and a PICTURE where you look your
     best. The moving part of the video (your mouth/face) is cut out, feather-edged
     and composited onto the picture — so the picture speaks with your real voice
     while keeping the look you chose. Position it with the sliders and check the
     Preview. Free, offline, your own footage on your own picture — nothing invented,
     no one else's face.
   AI SCENES: a selector sets how the AI-scene beats move — animated stills (default),
   REAL video — free cloud, or REAL video — local GPU (see §18). This only affects the
   AI scenes; your own footage/photo is NEVER AI-generated.

⏺ Recorder captures footage without leaving the app: your WEBCAM (with mic) or your
   SCREEN, at 720p up to 8K, with noise suppression / echo cancellation / auto-gain
   toggles. Using OBS? Start OBS's Virtual Camera and it appears in the camera picker
   automatically. Recordings are transcoded to MP4 and saved into Video Studio, ready
   to use anywhere — including as your Presenter Studio narration video.

-------------------------------------------------------------------------------
10. THUMBNAILS · LOGO/BRAND · PUBLISH · BATCH
-------------------------------------------------------------------------------
THUMBNAILS: Script Writer makes a 1280x720 thumbnail from a headline+style; in ✦ AI
   Command you can ask for a thumbnail with an AI-generated background.
LOGO: "🏷 Logo" row on a video -> pick a logo image, pick a corner, Apply (a
   transparent PNG looks best). INTRO/OUTRO: build short clips and use "Stitch" to
   join intro + video + outro.
PUBLISH: "▶ Publish to YouTube" writes an AI title/description/tags to your clipboard,
   opens YOUR channel's upload page, and reveals the file — drag it in. Free, no
   sign-in, no limits. It uses the same posting-text writer as "🏷 Posting text" below,
   so Shorts automatically get Shorts-appropriate wording.
POSTING TEXT: on any video or short, "🏷 Posting text -> YouTube / TikTok" writes a
   ready-to-paste title, description and hashtags with a Copy button next to each —
   uploading becomes copy-paste instead of writing from scratch.
BATCH: ✦ AI Command -> "📦 Batch" -> paste topics (one per line) -> "Make all" builds
   a video for each (up to 25). One failure won't stop the rest.
OVERNIGHT PLAN: same Batch box, but tick "🌙 Overnight plan" first (and pick how many
   Shorts per video). Now it ALSO cuts Shorts and writes posting text for every video —
   pick your topics, walk away, come back to publish-ready material instead of raw
   builds. Every result shows its posting text with a Copy button.

-------------------------------------------------------------------------------
11. FREE vs INTERNET vs ONE-TIME SETUP
-------------------------------------------------------------------------------
FREE + OFFLINE (anywhere, forever): building/looks/shapes, your voice + Natural voice,
   generated music & SFX, "create music only", captions, trim, watermark, stitch,
   export to all formats, remove/replace music on videos you MADE here, autosave.
FREE + INTERNET (keys built in, nothing to sign up for): AI writing/ideas/advisor/
   AI Command, AI images/visuals, thumbnails-with-AI-background, put-me-in-a-scene,
   online music removal.
FREE + INTERNET + ONE FREE SIGN-UP: REAL AI video — free cloud. Either a free
   Pollinations key (Quest Pollen, no phone — best here) or a free Puter sign-in; allowances are small
   and monthly. See §18.
FREE + ONE-TIME SETUP (optional; both already done on this PC): Natural voice (Piper)
   download; offline Demucs music separation (Python).

-------------------------------------------------------------------------------
12. FAQ
-------------------------------------------------------------------------------
Q: Do I need to install anything? A: No. Double-click the .exe. (Optional extras:
   the natural voice download and offline Demucs — both already set up here.)
Q: My videos vanished on a new PC. A: You copied only the .exe; copy the WHOLE folder.
Q: The AI says it's busy / fails. A: Free services get busy — try again; the app also
   auto-switches to a backup. Needs internet.
Q: Why is the computer voice robotic? A: That's the Windows voice — use YOUR voice
   (Voice studio) or the Natural voice.
Q: Photo/AI image is slow. A: Free image services queue; it shows your place. It's free.
Q: Can it auto-upload to YouTube? A: It prepares everything and opens your upload page
   so you drop the file — reliable and free. (Full auto-upload would force videos
   private until Google verifies the app.)
Q: Is my stuff private? A: Everything is saved only on your PC. AI features send your
   text/photo to the free online service to do the work. Offline features never leave.
Q: How do I use my own keys? A: Settings has fields (AI Horde for photos, MVSEP for
   online music removal, YouTube channel, paid Claude/OpenAI). All optional.

-------------------------------------------------------------------------------
13. TROUBLESHOOTING
-------------------------------------------------------------------------------
- Nothing on first double-click (portable exe): wait ~60s; don't double-click again.
  (The installed version opens in ~2s — run NIHILPOINTZERO-OS-setup.exe once to get it.)
- "My updates/changes didn't show up" / old look: check the BUILD BADGE in the sidebar
  (under "OS"). If it's older than expected you launched a STALE copy — this happens when
  you pinned the PORTABLE app to the taskbar/Start while it was running (Windows freezes
  the pin to a temporary unpacked folder). Fix: launch the DESKTOP shortcut or the .exe in
  the folder directly — or switch to the INSTALLED version, which can never go stale this
  way. The exe on disk is always the newest — the pin was pointing at old unpacked code.
- Live PSX Data with no internet / portal down: you'll see an amber banner and your last
  SAVED data (clearly dated). That's deliberate — real numbers from your last fetch, never
  invented ones. Fetch again when you're back online.
- "file not found" after moving: fully close and reopen; keep the whole folder together.
- An online feature errors: check internet, try again.
- A build fails at a weird resolution: it auto-retries safely; if not, use 1080p/4K.
- Writing stopped after changing provider: set it to "Free (online)" in Settings
  (needs no key), or make sure Ollama / your paid key is set up.
- An amber warning appears: "Your ... AI failed — this answer came from the free AI
  instead": your chosen paid/local AI could not answer, so the app used the basic free
  AI (answers will be noticeably weaker). Most common cause: a wrong, expired, or
  out-of-credit API key — open Settings and re-paste your key (an Anthropic key starts
  with "sk-ant-api..." and is very long; copy ALL of it). If your provider is Ollama,
  make sure Ollama is installed and running. The full technical reason is kept in the
  Activity Log. If you see NO warning, your chosen AI answered normally.
- A "real motion" scene came out as a still: the free cloud allowance ran out (it is
  small and monthly), the per-build cap was reached, or the service couldn't be
  reached — the build log says which. Falling back to AI stills is deliberate, so the
  build never breaks.
- A tab crashes / goes blank: the failure is contained to THAT tab — the rest of the
  studio keeps working. You get a plain-English message and a "Try this tab again"
  button, and the crash is written to Settings -> "Known Issues" so it's provable.
- Multiple copies open / won't launch: close all "NIHILPOINTZERO-OS" in Task Manager,
  then open once.

-------------------------------------------------------------------------------
14. WHAT'S ALREADY SET UP FOR YOU
-------------------------------------------------------------------------------
- AI brain: FREE (online) — works immediately.
- Photo-into-scene: free key built in.
- Online music removal: free token built in.
- Your YouTube channel saved for one-click Publish.
- Natural voice (Piper): installed on this PC.
- Offline music separation (Demucs): installed on this PC.

-------------------------------------------------------------------------------
15. PRO TIPS — GET THE BEST RESULTS
-------------------------------------------------------------------------------
- CONTROL YOUR SCENES/CARDS: put a heading in [SQUARE BRACKETS] on its own line in
  the script (e.g. [THE BIG IDEA]). Each becomes an on-screen section and, in Scene
  Studio, a scene. More brackets = more scene variety, less repetition.
- THE VOICE READS EXACTLY WHAT YOU TYPE: keep sentences short and clean; write
  numbers as words if you want them spoken clearly. Bracketed [HEADINGS] are NOT read
  aloud — they only drive the visuals.
- MOST PRO RESULT: record your OWN voice (🎙 Voice studio) over any build. Use "Redo
  from playhead" to fix one line without redoing everything.
- FOR SHORTS/REELS: Format 9:16 + a Bold or Cinematic Look + "Burn captions" — that
  combo performs best on muted, phone-first feeds.
- MUSIC: prefer "Replace" (it auto-ducks under your voice) over loud background music.
  Keep music as a bed, let the words lead.
- PUT-ME-IN-A-SCENE: lower the "Photo transform" slider to look more like yourself;
  raise it to transform more. Free image queues can be slow — be patient or run big
  jobs (Batch, many scenes) when you're away.
- SPEED: 1080p builds fastest; 4K is the sharp sweet spot; 8K is slow + huge. The free
  AI look uses the internet; the offline "Style presets/Look" builds are fastest.
- OLDER VIDEOS: features like exact music-removal and captions work best on videos
  built AFTER these upgrades — if an old one won't cooperate, rebuild it once.
- MOVE FAST: Ctrl-K jumps to any tab. Your work autosaves ("Saved ✓") and returns
  after a restart, so don't fear closing the app.
- BE ONLINE for the AI parts (writing, images, put-me-in-scene, online music removal);
  everything else works with no internet.

-------------------------------------------------------------------------------
16. EVERYTHING THIS STUDIO CAN DO (quick index)
-------------------------------------------------------------------------------
  WRITE:  AI ideas · AI scripts · your own Script Pad · dictate by voice (🎤) ·
          🎬 Producer — advise · rewrite (any language) · "Do it" (plans + runs real actions from any tab)
  DIRECT: ✦ AI Command (type a request, it does it) · 📦 Batch (many at once) ·
          🎞 Storyboard Director (shot-by-shot: length/scene/narration/caption/motion/crossfade +
          subject: AI scene / your photo composited / your clip / AI person + per-shot sounds)
  VISUALS:Looks (Clean/News/Cinematic/Bold) · animated titles + lower-thirds ·
          Ken-Burns of your images · free AI per-scene images · REAL AI video per scene
          (free cloud, Pollinations/Puter — §18) · Scene Studio storyboard (reorder/add/remove,
          pause, regenerate) · put YOUR photo in a scene · photo Beautify
  PRESENT:🎥 Presenter Studio (Real Video / Photo / ✨ Living Picture — YOU present,
          real footage only) · ⏺ Recorder (webcam or screen, up to 8K, OBS virtual camera)
  SHAPES: 16:9 · 9:16 (Shorts) · 1:1 · resolutions up to 8K
  VOICE:  your own recording (pause/redo/replace/add) · Natural (Piper) · Windows
  AUDIO:  generated music + SFX · free online music search · DJ timeline · create
          music-only MP3 · auto-ducking + peak limiter (no clipping) · waveform view ·
          remove/replace music · separate music from outside videos (online or offline Demucs)
  CAPTIONS: .srt export · burn-in
  BRAND:  logo watermark · intro/outro via Stitch · real + AI thumbnails
  EDIT:   Trim/cut · Stitch · AI Director (edit in plain English) · ✂ Timeline Editor (NLE)
  DATA:   📈 LIVE PSX prices → analysis (SMA/RSI/returns) → Excel → script → video ·
          🏦 NCCPL FIPI/LIPI file analysis → script · Charts (candlesticks/SMA/RSI, live or file)
  PUBLISH:export to YouTube/MP4/H.265/MOV/WebM · one-click "Publish" (opens your
          channel with title/desc/tags ready)
  EXTRAS: autosave + version history · Ctrl-K command palette · Setup Health panel ·
          toasts · Advisor chat · phone access over Wi-Fi · fully portable folder

Keep the .exe and the nihilpointzero-data folder together, be online for the AI
parts, and copy the whole folder to go anywhere. Enjoy.
===============================================================================

-------------------------------------------------------------------------------
17. NEW (2026-07-30): THE AI OUTAGE, FREE MUSIC, THE TRIM BAR, AND LIMITS
-------------------------------------------------------------------------------

WHY THE AI "STOPPED RESPONDING" - AND WHAT WAS DONE
The free online AI brain this app shipped with stopped being free. That was tested
directly, over and over: it now answers "402 Payment Required", "404 Model not found
- this is our legacy API", "429 Queue full", or returns an error page. Nothing on
your PC is broken and your internet is fine. They changed their business.

Why it FELT like a freeze rather than an error: the app used to ask that same dead
service TWICE for every question, waiting up to two minutes each time, and it never
wrote the failure down. So you waited a long time and got nothing.

What is different now:
  * Ollama - the free AI that runs on YOUR OWN PC - is now the automatic backup.
    You already have it, with two models installed. When the online one refuses,
    your answer quietly comes from your own machine instead. No key, no internet.
  * A service that refuses permanently is set aside for 30 minutes instead of being
    asked again before every single answer.
  * Every failure is written to nihilpointzero-data\logs\ai-errors.log with the time,
    the service, the error code, and the service's own words.
  * Settings has a new "Known Issues" panel showing that list, so a problem is
    provable instead of silently vanishing. It is read-only - nothing erases it.
  * Settings -> "Run full check" now genuinely tests the AI. It used to check a
    different web address than the one the app really uses, which is exactly why it
    kept showing a green light all through the outage.

If you want the most reliable free setup: keep Ollama installed and running.

PICTURES THAT NEVER APPEARED
Generated images were being made and saved correctly all along - the app's own
security rules were stopping the window from showing files from your disk. Fixed;
images, video previews and audio players across the whole app now appear.

THE NEW TRIM BAR
Open a finished video -> "Trim / cut". Instead of typing numbers:
  * Tap anywhere on the bar to move the nearest marker there.
  * Drag either marker to fine-tune. Arrow keys nudge; hold Shift for bigger steps.
  * The selected stretch is shaded RED when you're cutting it out, GOLD when you're
    keeping only it.
  * "Jump to start"/"Jump to end" move the player so you can check your marks.
  * Pressing Apply asks "Remove this section?" with Yes/Cancel BEFORE anything is cut.
  * A new video is always created - your original is never touched.
If you preferred typing exact times, that's still there under "Type exact times".

FREE, COPYRIGHT-SAFE BACKGROUND MUSIC
Under the trim bar is a green "Music track" lane:
  * Tap it to drop music, or drag across it to choose exactly where music plays.
  * Tapping opens a small picker right there - no separate screen.
  * The AI reads your script and picks the mood for you (tense, uplifting, corporate,
    and so on), then shows matching tracks. You can also search a mood yourself.
  * Tap the round button to preview a track; tap "Use" to take it.
  * Every track shows its licence in plain words: "no credit needed" (use it and
    forget it) or "credit needed" (put the artist's name in your description).
    Credit-free tracks are listed first on purpose.
  * Music comes from Pixabay (add a free key in Settings) and Openverse (needs no key
    at all, so this works out of the box). All safe for monetised YouTube.
  * If neither service can be reached, you're told and the video is simply left
    without music - it never fails the whole job.

"NO VOICE / SILENT" NARRATION
Video Studio -> Narration voice -> "No voice / silent". The video is built with NO
narration at all, timed to how long your script would take to read aloud, so you can
record your own voice over it afterwards in Voice studio.

RECORDING YOURSELF - NOW IN THE RIGHT PLACE
Voice studio (under any finished video) no longer records only sound:
  * Choose "Voice only" or "Voice + camera".
  * Tick "Play the video while I record" and the finished video rolls as you perform,
    so you're genuinely recording over it rather than guessing. WEAR HEADPHONES - on
    speakers your microphone will also pick up the video's own sound.
  * A camera take can be saved straight into Video Studio (optionally cleaned up), or
    you can keep just its sound as the video's narration.
For a quick voice-over-video take you never have to leave Voice studio. (The separate
⏺ Recorder tab still exists and is for bigger captures — webcam or full-screen
recording up to 8K; see §9e.)

CAPTIONS AND YOUTUBE CHAPTERS - ONE SWITCH, OFF BY DEFAULT
There is now a single tick-box in Video Studio: "Also make subtitles + YouTube
chapters". It is OFF unless you tick it, and when it's off neither is created.
Being straight with you: captions were NEVER being forced on - they only ever ran
when you clicked the Captions button yourself. Chapter markers didn't exist at all
until now. Both are now behind one visible switch. Chapters come with a Copy button
so you can paste the timestamps into your YouTube description.

WHAT THIS PC CANNOT DO - THE HONEST ANSWER
Real AI "motion video" (LTX-Video, Wan 2.2, CogVideoX) and "talking photo" tools
(SadTalker, LivePortrait) CANNOT run on this laptop. Not slowly - not at all.

Those models need a dedicated NVIDIA graphics card with its own video memory. This
PC has Intel graphics built into the processor, which borrows ordinary system memory
and has none of the special hardware those models require. The lightest one asks for
6GB of dedicated video memory; there is none here. No setting changes that.

So instead of pretending, the app now:
  * Checks your graphics card when it starts and tells you plainly, in Settings and
    before you start a build, what can and cannot run.
  * Labels the slideshow honestly as "Photo slideshow (AI images)" - described as a
    moving photo slideshow, NOT filmed motion.
  * Offers 16 distinct looks instead of 5: five cinematic (modern film, film noir,
    blockbuster, vintage 70s, documentary), four cartoon (bold flat, 3D animated,
    comic book, watercolour storybook), four anime (modern key visual, retro 90s,
    painterly pastoral, dark seinen), plus neon, minimal and infographic.
If you ever run this on a PC with an NVIDIA card, the check notices and the motion
video option becomes available on its own.

UPDATE (2026-07-31): real AI motion video IS now possible here — through the free
CLOUD tier, not this PC's hardware. The paragraphs above stay true for LOCAL models.
See §18 for what's new and the honest catches.

-------------------------------------------------------------------------------
18. NEW (2026-07-31): REAL AI VIDEO — FREE CLOUD TODAY, LOCAL GPU WHEN THE
    HARDWARE EXISTS
-------------------------------------------------------------------------------

REAL MOVING VIDEO, FREE — TWO ROUTES
Video Studio's "Video look (engine)" list has a new tier: "REAL AI video — free
cloud". It generates REAL moving video for each scene — not a photo slideshow.
There are two free ways to power it; pick one in Settings -> AI Video:

ROUTE 1 — POLLINATIONS (recommended on this PC; NO phone number):
  * Sign up free at enter.pollinations.ai with GitHub or email, create a key, and
    paste the SECRET key (sk_…) into Settings -> AI Video. (The pk_ "app" key is
    often created with no models allowed — the sk_ key is the one that works.)
  * Free Pollen is CLAIMED from that dashboard's QUESTS tab. Quests are
    retroactive — if you already qualify, just claim: "Create your first API key"
    alone pays 0.25 Pollen, and the default video model (wan-fast) costs about
    0.05 Pollen for a 5-second scene, so that one claim is ~five real-motion scenes.
  * The "Test key" button checks your key and shows your Pollen balance without
    spending anything — and warns you plainly when the balance is 0.

ROUTE 2 — PUTER (Google's Veo; no key at all):
  * A sign-in window pops up during the first build; sign into a free Puter
    account there (it may ask again after an app restart).
  * Heads-up: Puter's verification rejects some countries' phone numbers —
    Pakistani numbers included. If their sign-up won't finish, use Route 1.
  * The free allowance is SMALL and MONTHLY.

True for both routes:
  * When an allowance runs out, scenes automatically fall back to AI still images
    and the build log says why. Nothing breaks.
  * By default only up to 5 scenes per build get real motion — this protects the
    free allowance. The rest of the scenes use AI stills. Adjustable in Settings ->
    AI Video ("Real-motion scenes per build").
  * Each real-motion scene takes MINUTES to generate. Expect a slower build.

Where it lives: Video Studio ("Video look (engine)") · Scene Studio ("Video look")
· Storyboard Director ("Scenes:" selector) · Presenter Studio (the AI-scene
selector — your own footage/photo is never AI-generated).

THE LOCAL GPU TIER — REAL, BUILT, AND WAITING FOR HARDWARE
"REAL AI video — local GPU (ComfyUI)" is now a real integration with ComfyUI, the
standard local AI video server (default address http://127.0.0.1:8188).
The hard truth has NOT changed: this PC (Intel graphics inside the processor, no
NVIDIA card) cannot run it — not slowly, not at all.
What HAS changed: instead of being hidden, the option is now VISIBLE but greyed
out — "Requires NVIDIA GPU — not detected on this system" — and everything can be
configured in Settings -> AI Video today, so it unlocks by itself the day this PC
(or your next one) has an NVIDIA card. Settings also says which model fits which
card:
    8GB   -> AnimateDiff / Wan 2.1 (1.3B)
    12GB  -> LTX-Video / LTX-2
    16GB  -> LTX-2.3 (recommended)
    24GB+ -> Wan 2.2 / HunyuanVideo 1.5
Advanced: you can point it at your own ComfyUI workflow file — export it in ComfyUI
via "Save (API format)" and put {{PROMPT}} where the prompt text goes.

EVERY AI VIDEO ENGINE NOW FALLS BACK SAFELY
If a real-video engine can't run — offline, allowance used up, no server — the build
automatically continues as the photo slideshow and the status log states the reason.
A build never breaks because of these engines.

A CRASHED TAB CAN'T TAKE THE STUDIO DOWN — AND IT'S ON THE RECORD
The "Known Issues" panel in Settings used to record only AI service failures. Now,
if a tab crashes, the crash is contained to THAT tab: the rest of the studio keeps
working, you get a plain-English message and a "Try this tab again" button, and the
failure is written to the same Known Issues log — so it's provable, not a mystery.

